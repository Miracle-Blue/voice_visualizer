name: voice_visualizer
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_soloud: ^3.4.7

  logbook: ^0.3.3

  shader_buffers: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/music/

  shaders:
    - assets/shader/voice_vi_2d.frag
    - assets/shader/voice_vi_3d.frag
